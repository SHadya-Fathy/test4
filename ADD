#include <iostream>
using namespace std;

int sum(int x, int y) {
    return x + y;
}

int main() {
    int x, y;
    cout<<"enter number1 is:";
    cin >> x ;
    cout<<"enter number2 is:";
     cin >> y ;
    cout << sum(x, y) << endl;
    return 0;
}

